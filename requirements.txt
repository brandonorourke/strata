# Local package
-e .

# Web server (not used yet — keep >= unless you're actively building the API)
fastapi==0.115
uvicorn[standard]==0.30

# Database + ORM
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0

# RSS + HTTP + Parsing
feedparser==6.0.11
httpx[http2]==0.27.0
trafilatura==1.7.0

# Environment
python-dotenv==1.0.1

# LLM client
openai==1.55.3
